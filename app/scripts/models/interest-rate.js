/*global define*/

define([
  'underscore',
  'backbone'
], function (_, Backbone) {
  'use strict';

  var InterestRateModel = Backbone.Model.extend({
    defaults: {
    }
  });

  return InterestRateModel;
});
